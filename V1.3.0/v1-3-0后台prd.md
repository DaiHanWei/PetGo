# TailTopia 管理后台 PRD — V1.3.0

**版本：** 1.3.0
**状态：** 定稿（2026-09-01；final 前盘点复核：`dev/dev_1.1.6` @ `7ac9a4dd` 与全量清单快照一致，零漂移）
**定稿后修订：** 2026-09-03 增补 AB-20A 虚拟账号评论（暖贴）——UI 阶段产品新增需求，见 §6
**上游依赖：** V1.1.0 管理后台 PRD（`../V1.1.0/v1-1-0后台prd.md`）、V1.3.0 App PRD（`1-3-0prd.md`）

本文档记录 V1.3.0 新增或变更的管理后台功能，既有功能不在此重复描述。

**编号规则：** AB 编号沿用「功能编号跟着功能走、不随文档迁移变更」的既定原则。本版本新分配 **AB-15A**（首页数据看板，模块 15 为内容运营管理——看板虽挂后台首页、但服务对象是内容运营，归此模块）、**AB-16A**（后台账号管理增强，扩展模块 1 安全）、**AB-17A**（场所内容管理，新模块 17，2026-08-28 确认纳入）、**AB-18A**（KTP 模块高清图解锁定价配置——含 KTP 卡 HD 与护照两样式，扩展既有运营配置，2026-09-01 新增并同日更名）、**AB-19A**（后台 IA 与操作体验重整，2026-09-01 纳入，方案梳理中——见 §5）、**AB-20A**（虚拟账号评论/暖贴，扩展评论管理页，2026-09-03 新增——见 §6）、**AB-21A**（角色配置功能，扩展模块 1「安全 → 后台账号」，2026-09-04 新增——见 §7）。

**与 App 端的依赖关系：** AB-15A / AB-16A 与 App 端各 FR 互不依赖、可独立交付、不造成任何空转；**AB-17A 是 App 端 FR-112 的同版本硬配套**（2026-08-28 已确认纳入）——与 FR-112 同版本交付；**AB-18A 随 FR-120 交付**（缺失则护照样式解锁只能用硬编码价格，不阻塞上线但运营不可调价）；**AB-20A 与 App 端零耦合**（复用既有评论链路与虚拟账号池，App 不改一行）；**AB-21A 与 App 端零耦合**。

**页面位置与流程约定（2026-08-28 新增工作规则）：** 本文档起，每条后台功能必须写明**落在后台的哪个页面/菜单位置、操作流程从哪里进从哪里出**，减少工程错误理解。

**后台须配 UI/UX（2026-09-01 新增工作规则）：** 自本版本起，后台功能与 App 端同样走 UI/UX 设计流程（先 PRD、再 UI 稿、定稿后交开发）——**推翻 1.2.0 时「后台不单独出 UI 设计、技术按 PRD 界面要求直接实现」的旧约定**。本版本 4 条 AB 均须出 UI 稿。

**技术底座约束（各条 AB 通用）：**

- 管理后台为 Thymeleaf 服务端渲染 + htmx（vendored，无 CDN 依赖）。AB-15A 的图表能力**不得引入 CDN 外链**——图表库须 vendored 落 `static/admin/vendor/`（如 Chart.js）或自绘 SVG，选型由技术方案定
- 所有写操作沿用统一审计入口（`AdminAuditService.record`，同事务、哈希链），审计动作码进 `AuditActions` 常量表（UPPER_SNAKE 惯例）
- **三语操作界面（2026-09-01 确认为硬约束）**：后台支持 中文 / English / Bahasa Indonesia 三语——现状基建已齐（i18n 四语言包各 ~2053 key 零缺失 + 顶栏语言切换器 `?lang=zh_CN/en/id`，按会话生效）。本版本全部新增/改动文案（AB-15A~19A、37 页重构、图表口径提示、护照样式名等）**一律走 message key 并同步交付三语**，任何一页写死单语即验收不通过；新增 key 的三语文案表随各批 UI 稿交付，印尼语术语以现有 id 语言包为基准保持一致；账号级语言偏好持久化不在本版（现状会话级记忆维持）
- 高危操作的二次确认统一沿用既有 `data-confirm` 模式（admin.js），表单校验错误沿用行内 err 提示模式
- **时区：** 后台所有日期展示与"自然日"切分统一按 **WIB** 标注与计算（沿用后台既有惯例），且须与 SQL/数据库的日切口径一致——两处切日不一致会导致看板与 SQL 直跑对不上

---

## 0. 后台全景与运营操作逻辑（2026-09-01 按代码梳理）

> 应产品要求，本节把管理后台的现有功能全貌与运营的日常操作逻辑整理成文——此前后台能力散落在 V1.1.0 / V1.1.4 / V1.2.0 三份后台 PRD 里，没有一处能看到全局。本节按 2026-09-01 的 `layout.html` 导航与代码现状梳理。**逐页面、逐操作的全量盘点（63 页 / 128 个写操作，基于 `dev/dev_1.1.6` 超集分支）见配套文档 `后台功能全量清单-20260901.md`**。
>
> **2026-09-01 拍板：后台 IA 与操作体验重整纳入本版本（AB-19A）**——不动任何功能、不增删任何能力，只重排导航与页面组织、优化运营操作动线。0.3 的问题清单即其输入，重整方案与产品共同梳理后定稿。

### 0.1 导航全图（10 组 33 项 + 2 个无导航入口页面）

| 导航组 | 菜单项 | 给运营干什么 |
|---|---|---|
| **概览** | 概览（首页） | 四模块聚合数字速览 → **本版本 AB-15A 替换为数据看板** |
| **内容** | 种子内容发布 / 批量内容 / 内容管理 / 评论管理 / 人工复核 / 顶置管理 / 内容标签 | 内容供给（种子单发/批量+工作台+定时）、全量内容与评论的巡查处置、审核复核、Feed 顶置坑位配置、装饰标签打标 → **本版本新增「场所管理」（AB-17A）** |
| **用户运营** | 用户 / 被举报用户 / 用户标签 | 用户查询与详情、账号举报处置（警告/封号）、运营标签配置与分配 |
| **兽医** | 兽医账号 / 在线状态 / 未成功请求 / 兽医评分 | 兽医账号创建与资质录入、在线监控、掉单排查、评分查看。⚠️ 兽医线整体延期后为**低频区** |
| **问诊异常** | 异常工单 / 历史会话查询 | 问诊纠纷与异常处置、会话取证。同上，低频区 |
| **客服** | 客服工单 | 用户求助工单处理 |
| **退款** | 退款管理 | 退款审核与执行 |
| **运营** | 运营配置 / 算法参数 / 兽医订单 / AI 订单 / 兽医月结 / 支付记录 / 红色超额 / 运营发布身份 | 定价与开关配置（→ **本版本 AB-18A：KTP 模块高清图解锁定价组**）、推荐算法参数（不对运营开放）、两类订单查询、月结打款、支付流水、红色评级超额监控、种子发布身份池 |
| **安全** | 操作审计 / 后台账号 | 审计链查询、后台账号全生命周期（→ **本版本 AB-16A 补改名/换绑/self 护栏**） |
| **电商（Toko）** | 商品管理 / 顶部 Banner / 库存管理 / 订单履约 / 异常订单 / 退货审核 / 开封判例 / 复购引擎效果 / 销售与毛利 / 库存周转 / 对账 / 服务范围与运费 | 1.2.0 随 Toko 上线的自营电商全链路：商品/库存/履约/售后/经营分析/物流配置。**12 项，已是最大导航组** |
| （无导航入口） | 内容统计（`content-stats`，AB-3G）· 定时发布（`content-schedules`，AB-3L） | ⚠️ 这两页在导航里**没有菜单项**（只能从页内链接或直链进入），运营可发现性差——见 0.3 问题清单 |

### 0.2 运营的四条日常工作流（现状动线）

1. **早巡（每天开工）**：概览看数 → 人工复核清队列 → 评论管理巡查 → 被举报用户处置 → 客服工单。**痛点：五个页面分属三个导航组，无统一「待办」视图**，每天靠记忆逐页点
2. **内容供给（每周节奏）**：运营发布身份选马甲 → 种子内容发布/批量工作台 → 定时发布排期 → 顶置管理换坑位 → 内容标签打标（选品参考看内容统计 AB-3G）。**痛点：动线里的「定时发布」「内容统计」恰好是两个无导航入口的页面**
3. **数据复盘（每日/每周）**：现状只有概览四个数字 + 内容统计双口径 → **本版本 AB-15A 数据看板补上趋势复盘的主阵地**
4. **售后与钱（按需）**：退款管理 / 支付记录 / 兽医月结 / Toko 退货审核与对账。金额相关操作全部有审计链兜底

### 0.3 结构性问题清单（本版本不动，供后续 UI/UX 重整拍板）

1. **【建议】审核处置动线分散**：人工复核、评论管理、被举报用户、异常工单、客服工单五处队列分属三组——早巡靠逐页点，漏看无提示。方向：统一「待办中心」或导航组按「审核」聚合，并给各队列加未处理计数角标
2. **【建议】「运营」组已成杂物抽屉**：定价配置、算法参数、订单查询、月结、支付流水、身份池六种性质混在一组。方向：拆「订单与资金」「配置」两组
3. **【建议】兽医 + 问诊异常两组共 6 项，在兽医线延期后是低频区**，却占两个一级组位。方向：合并为一组「兽医与问诊」折叠置后
4. **内容统计 / 定时发布无导航入口**（0.1 表尾）——不论 IA 是否重排，这两项至少该补菜单项，成本极低
5. **权限勾选面板与导航不同构**：权限按「查看/编辑」两大组罗列 code，运营语言是「管哪个页面」——给 STAFF 配权限时对照成本高。方向：权限面板按导航组重排
6. 电商组 12 项独大（1.2.0 一次并入）——Toko 上线后按使用频率再看要不要分层，本版本不动

---

## 1. AB-15A：运营后台首页数据看板（替换现首页）

**现状：** 后台首页为「运营概览」（V1.1.0 Story 9.10 / AB-1.1-01）——用户/订单/内容/兽医四模块的实时聚合数字表格 + 种子发布链接入口。数字为「当前时点累计」，无时间维度、无趋势、无图表。

**页面位置：** 后台首页 `/admin`（左侧导航「概览」，登录后默认落地页）——在原页面原位替换，不新增菜单项。

**本条改版：** 首页整体替换为**内容运营数据看板**——以日为粒度的时间序列指标 + 图表呈现。**指标口径以 `../3.数据埋点/数据查询/内容运营所需数据.sql`（2026-08-31 版）为准，共 18 项指标**（原 14 项 + 付费 4 项；该 SQL 头部注释即口径权威定义，本节不复述细节、只列清单与分组。⚠️ 该 SQL 后续再改动时，本节清单须同步——两处走散即失效）。

### ① 指标清单与看板分组

| 分组 | 指标（共 18 项） | 图表形式 |
|---|---|---|
| 用户增长 | 新增用户数 · 总安装用户数（累计） | 新增=柱状（按日），累计=折线（同图双轴或分图，技术方案定） |
| 建档转化 | 新增建档用户数 · 累计建档用户数 · 当日发 diary 的建档用户数 | 同上结构；「发 diary 建档用户 / 累计建档用户」可加比率辅助线 |
| 内容生产 | 发帖用户数 · 新增帖子数 | 按日柱状，双序列同图 |
| 互动质量 | 有互动的帖子数 · 今日沉默帖子数 · 总互动得分 · 今日帖子总得分 · 全部帖子全量平均分 · 当日互动帖子平均得分 · 总互动得分（全部历史，累计） | 计数类按日柱状/折线；两个平均分为折线（看趋势不看绝对值——SQL 注释已写明全量平均分会被存量摊薄）；累计类折线 |
| 付费（2026-08-31 随 SQL 更新新增） | 付费人数·现金到账 · 付费次数·现金到账 · 付费人数·含 PawCoin 消费 · 付费次数·含 PawCoin 消费 | 人数/次数双序列按日柱状，两口径分图或切换；两口径差异提示必须展示（SQL 口径 6：「先前充值、今天用余额买」的人①不算②算） |

- **要求以图表呈现，不是照搬表格**——每个分组一张图表卡片；图表下方可附当日数值明细行，但明细不能替代图表
- **页面结构（自上而下）**：时间范围选择器（全局作用于全部图表卡）→ 五张分组图表卡（用户增长 / 建档转化 / 内容生产 / 互动质量 / 付费）→ 底部「当前时点总量」紧凑摘要行 + 种子发布链接（见 §③）
- 时间范围选择（2026-09-01 拍板简化）：**近 7 天 / 近 30 天 两档，用户自行切换**（默认 7 天）；不做 14 天档与自定义起止；切换用 htmx 局部替换图表区，**不整页刷新**
- **付费卡的两口径**：卡内 tab 切换「现金到账 / 含 PawCoin 消费」，默认现金到账；口径差异提示常驻卡底（非 hover 才可见）
- 图表交互：hover/点按显示 日期 + 数值 tooltip；其余交互（图例开关等）用图表库默认能力即可，不做定制要求
- **缺数据日的呈现**：预聚合缺行的日期在图上显示为**断点/缺口**，不得画成 0（画 0 会被误读为"当天没有数据量"）；图表卡加载中显示骨架、加载失败显示卡内重试按钮
- 每项指标须带口径提示（hover/图标展开），文案直接取自 SQL 头部注释的口径说明，**不另写一套**——两处口径描述走散是本条最大的长期风险

### ② 口径与刷新规则（与 SQL 严格一致）

- 所有指标的日截止点为**该自然日 24:00**（即只展示到「查询当天 00:00」为止的完整自然日；当天数据不完整、不展示）——与 SQL 日报口径一致，图上最后一根柱永远是昨天
- 排除口径一致：`role=ADMIN`、`google_sub` 前缀 `admin:` / `virtual:` / `seed-tailtopia-` 的账号不计入用户类指标；帖子只计 `PUBLISHED` 且未删除；评论只计未删除
- **同一数字两处必须相等**：看板任一指标在任一天的数值，必须与直接执行该 SQL 得到的数值完全一致（SQL 注释已保证按天口径可复现）。验收即用这条对比
- **历史回填**：SQL 文末带一个「2026-07-17 起逐日付费人数/次数」的一次性补历史查询（与日报口径完全一致）——看板初始化时用它回填付费四项的历史序列，其余指标按预聚合逐日物化自然积累
- **性能实现自由、口径不自由**：SQL 中全量平均分等指标为 LATERAL 全表打分、直接每次实时跑不可行，允许做**日粒度预聚合**（每日跑批物化昨日一行，历史行不再变——SQL 口径本身保证了同一天的数字不随后续互动上涨，天然适合物化）。预聚合表结构与跑批时机技术方案定

### ③ 边界

- **次日留存不承载**——库里没有登录/打开 App 日志表（登录事件只进 PostHog 不落库），SQL 明确排除了该指标，看板同样不做；看板页可放一行说明 +「去 PostHog 查看留存」的提示文案（不做外链跳转集成）
- 原首页的四模块实时聚合数字**降级为看板页底部的一行紧凑摘要**（保留「当前时点总量」的速览价值），种子发布链接入口保留
- 不做导出（AB-3G 已有带导出的互动积分统计，管理层汇报走那边）；不做指标自定义/拖拽布局
- **权限：** 看板对所有后台账号可见（与现首页一致，不新增 permission_code）——数据为聚合数字、无 PII

---

## 2. AB-16A：后台账号管理增强（扩展模块 1「安全 → 后台账号」）

**现状（2026-09-01 二次代码核验，再次订正）：** 「安全 → 后台账号」已具备——创建账号、**修改模块权限**、**修改岗位角色**（`changeRole`：账号在「岗位角色」体系内切换，含模板角色与 SUPER_ADMIN；已带两道护栏——超管上限 5、不降级最后一个在职超管；审计 `ACCOUNT_ROLE_CHANGED`）、**停用**（已带「不停用最后一个在职超管」护栏）、**重新激活**。此前 Roadmap 记的「只有创建与停用」与本文档初稿写的「改账号类型待新增」**均不准确——改权限/改角色/停用恢复全部已上线，本条不重复建设**。既有规则补记：**超管上限 5（ACTIVE 口径）**、账号无 delete 方法（永不硬删）。

**「重置密码」撤销（2026-08-26 拍板）：** 后台账号走 Lark OAuth 登录，STAFF 无密码；仅 bootstrap 预置的超管有一条 env 注入的紧急账密（BCrypt，运维层管理）。「重置密码」在当前架构下不成立，从本条范围移除。

**页面位置与流程：** 「安全 → 后台账号」既有页面 `/admin/accounts`——新动作全部做成**账号列表行内操作**，与既有「改角色 / 改权限 / 停用 / 重新激活」按钮并列；点击后行内展开或弹层完成，提交走既有 PRG 模式回到本页并显示结果横幅。不新增页面。

**本条实际补齐：两个新动作 + 两条缺失护栏**：

### ① 修改显示名

- 操作权限：`SUPER_ADMIN` 或持 `admin.create_account`（与创建/改权限同门槛）
- 规则与创建时一致（非空、≤100 字符）；改名不影响该账号任何会话与权限
- 审计：`AuditActions` 新增 `ACCOUNT_RENAMED`（命名沿用 ACCOUNT_* 惯例，下同），summary 记录旧值 → 新值

### ② 换绑 Lark 邮箱（高危）

**场景：** 人员邮箱变更、账号移交。没有这个动作时的现状是「停用旧账号 + 新建账号」，权限配置须手工重配且历史审计上下文断裂。

- 操作权限：**仅 `SUPER_ADMIN`**
- 新邮箱须通过与创建时相同的格式校验，且不得与任何既有账号（含已停用）的邮箱重复
- **换绑即时生效**：旧邮箱的 Lark OAuth 立即无法命中白名单；该账号的**既有登录会话立即全部失效**（强制重新登录——换绑语义上是身份移交，不能让旧持有人的活跃会话继续存活）
- 页面须二次确认（输入新邮箱后弹确认框复述「旧邮箱 → 新邮箱，旧邮箱立即失去访问权限」）
- 审计：新增 `ACCOUNT_EMAIL_REBOUND`，summary 记录旧邮箱 → 新邮箱；**同时触发超管安全告警**（`AdminAlertService.alertSuperAdmins`，与既有高危事件同管道）
- **bootstrap 预置超管不可被换绑**（其身份由 env 配置锚定，改由运维层处理）

### ③ 补齐两条缺失的 self 护栏（2026-09-01 代码核验：既有实现确实没有）

- **不能停用自己**、**不能修改自己的岗位角色**——防误操作自锁（超管把自己降级/停用后无人能恢复的场景，现有「最后超管」护栏只防得住只剩一人的情况，防不住多超管时的自伤）
- 实现位置：`deactivate` / `changeRole` 各加一条 `actorAccountId == accountId` 拒绝判断，拒绝时明示原因（沿用 `admin.err.*` 错误码模式）
- 「最后一个在职超管不可停用/降级」护栏**已存在**（A3，含幂等），本条不重复建设

### ④ 不变的原则

- **账号只可停用、永不删除**（保审计完整）——本条三个新动作均不触碰该原则
- 所有操作经统一审计入口 `AdminAuditService.record`（同事务、哈希链），不得绕过
- 页面沿用既有后台账号页（`admin-accounts.html`）扩展，不新开页面

---

## 3. AB-17A：场所内容管理（新模块 17，2026-08-28 确认纳入）

> **来源与状态：** 本条为 App PRD 撰写中新识别的 **FR-112 硬配套缺口**——FR-112 是先发后审的 UGC 功能，没有后台则违规/虚假场所无法下架、重复场所无法合并、运营冷启动数据无录入通道。**2026-08-28 产品拍板：纳入本版本**，与 FR-112 同版本交付。

**对应用户端 PRD：** `1-3-0prd.md` FR-112。

**页面位置与流程：** 左侧导航「内容」分组下新增菜单项**「场所管理」**，页面 `/admin/places`。页内结构：顶部筛选/搜索条 + 场所列表表格 + 右上「新建场所」按钮（冷启动预置录入用）。行内操作：编辑（行内展开表单）/ 下架 · 恢复（二次确认后 PRG 回本页）/ 合并（点「合并」→ 弹层内搜索选择保留场所 → 确认执行）。照片/评论的删除在「查看详情」展开区内逐条操作。被举报场所不在本页处理——沿用既有**举报审核队列**页面（处置对象类型新增「场所」，处理动作联动本模块的下架能力）。

### ① 场所列表

- 列表列：名称 / 类型 / 标签 / **文字地址** / 标记人 / 照片数 / 评论数 / **打卡数** / **推荐·不推荐计数** / 状态（上架 / 已下架 / 已合并）/ 创建时间
- 默认按创建时间倒序；支持按名称或地址搜索、按类型与状态筛选；分页沿用后台既有分页模式；空列表与搜索无结果各给空态文案
- 每行「查看详情」展开区：全部照片（缩略图 + 上传者 + 逐张删除钮）· 全部评论（正文 + 作者 + 二元态度 + 逐条删除钮）· 坐标与地址 · 打卡计数
- 被举报的场所进**既有举报审核队列**（复用内容举报处置流，处置对象类型新增「场所」；队列内可执行的处置动作 = 下架场所 / 删除照片 / 删除评论 / 驳回举报，处置能力与本页同源）

### ② 处置动作（全部落审计日志，动作码沿用 `AuditActions` 惯例新增 PLACE_* 系列；合并为**不可逆**操作，确认弹层须复述「保留 A、并入 B、不可撤销」）

| 动作 | 说明 |
|---|---|
| 编辑 | 修正名称 / 类型 / 标签 / 描述 / **文字地址 / 坐标**（预置录入填错须可修）；**标记人不可改** |
| 下架 / 恢复上架 | 下架后用户端列表与详情不可见（直链进入显示「场所不存在」统一文案）；软删，不物理删除；**已产生的护照章与 Diary 打卡 banner/帖内场所条保留不删**，仅跳转落「场所不存在」（2026-09-01 拍板）；**恢复上架后上述跳转自动恢复**，无需任何数据修复 |
| 删除单张照片 / 单条评论 | 场所本身没问题、个别 UGC 违规时的精细处置。照片**允许删至 0 张**（违规照片必须能删干净，用户端以占位图兜底）；删除评论时其二元态度一并从计数中扣除 |
| 合并重复 | 选择保留场所 + 被合并场所 → 被合并场所的照片、评论、**打卡记录**归入保留场所、本体下架并记录合并指向（直链跳转保留场所）；**两边都盖过章的用户，护照章并成一枚、到访次数相加**（2026-09-01 拍板）。MVP 不做自动查重，靠运营人工发现（用户举报「重复」类目辅助） |

### ③ 运营预置录入（冷启动通道）

- 后台直接创建场所：字段与用户端一致（名称/类型/标签/照片/描述/文字地址），位置=**地图选点**（vendored 底图，搜索地址定位 + 拖动大头针，坐标随选点自动写入并只读展示——`[2026-09-01 修订]` 原「经纬度手工填写」被否：与 App 端同交互、不手填数字）——雅加达 10-20 个核心场所由此录入，须在版本上线前完成（Roadmap 关键提醒表）
- 预置场所的标记人指定为运营官方账号（从 AB-3I 运营发布身份池选择）
- 不做批量导入（10-20 条手工录入成本可接受；量级上来再议）

### ④ 权限

- 新 permission_code：`place.manage`（读 + 全部处置动作，不再细拆——模块内动作粒度拆分留待有多人分工诉求时再做）；预置录入同码

---

## 4. AB-18A：KTP 模块高清图解锁定价配置（2026-09-01 新增，随 FR-120；同日按产品反馈更名并纳入 KTP 卡 HD 行）

**对应用户端 PRD：** `1-3-0prd.md` FR-120——护照集章页样式按款一次性解锁去水印，定价默认与 KTP 高清下载一致（当前 Rp5,000），须运营可配。

**页面位置与流程：** 既有**运营配置页**的配置组更名重组为「**KTP 模块高清图解锁定价**」——三行：KTP 卡高清下载（自原「定价配置」组迁入）/ 护照·护照内页 / 护照·登机牌；每行样式名只读 + 价格输入；**每卡自带保存钮（未修改禁用 → 修改激活 → 高危确认弹层复述新旧值 → 提交）**，沿用现状每组各自提交模式。不新增页面、不新增菜单项。

- 新样式上架时由工程随发版在配置组内加行（样式本身为发版内容，后台只管价格）
- 改价即时生效，只影响**新发起**的解锁购买；已解锁用户不受任何影响（一次性永久语义）
- 价格默认值 = 当前 KTP 高清下载价（行旁展示 KTP 当前价作参考）；两者**各自独立配置**，改 KTP 价不联动护照价
- 价格校验：非负整数（IDR）；**允许配 0 = 该样式限时免费解锁**（运营活动用），0 价解锁同样落购买记录。`[2026-09-01 代码核验]` **现有 KTP HD 购买链路无 0 价短路**（price 直接进 PawCoin 扣减 / QRIS 建单，0 元建单大概率被渠道拒）——**0=限免需要工程新增零价短路**（price==0 → 跳过支付直接发放解锁并落 0 元购买记录），不是纯复用
- 所有改价操作经统一审计入口落审计日志
- 权限：沿用既有定价配置项的 permission_code，不新增

---

## 5. AB-19A：后台 IA 与操作体验重整（2026-09-01 纳入本版本，方案梳理中）

**原则（已拍板）：** **不动任何功能**——不增删任何页面能力与操作端点，只重排导航分组、页面组织与操作动线，优化运营的使用体验。功能底账以 `后台功能全量清单-20260901.md`（63 页 / 128 写操作）为准，重整前后**逐操作核对不丢一项**即为核心验收标准。

**输入：** §0.2 运营四条日常工作流 + §0.3 六条结构性问题清单 + 全量清单的新发现。

**逐页规格：** 每页「包含什么内容 / 怎么展示 / 什么规格」承载于配套文档 **`后台重构逐页规格.md`**，按新导航组分批产出、分批过审（第一批 = 待办中心组 8 页，兼作标准细度样例）。

### ① 新导航结构（2026-09-01 拍板）

| 新导航组 | 收进来的页面 | 变化说明 |
|---|---|---|
| 📊 概览 | 数据看板（AB-15A） | 不变 |
| 📥 **待办中心（新）** | 统一复核工作台（内容送审/内容举报/昵称·宠物名审核/头像审核四页签）· 被举报用户 · 异常工单 · 客服工单 · 退款管理（共 5 项；原独立举报队列页 `reports` 系被统一队列取代的遗留页，**2026-09-01 拍板退役**、端点保留）；`[2026-09-08 修订]` Toko 退货审核/Toko 异常订单不再收进本组，按现状随电商整体归入商城组 | 早巡动线归一；组名挂未处理总数角标、各项挂各自计数 |
| ✍️ 内容 | 种子发布 · 批量内容（含「发布排期」页签——原排期管理孤儿页并入，2026-09-03 拍板）· 内容管理 · 评论管理 · 顶置 · 内容标签 · 场所管理（AB-17A） | 排期并入批量内容页签（排期 100% 由批量产生，不再单设菜单项）；~~内容统计~~ dev 分支已撤页（点赞数据并入内容管理列表）；审核/举报移去待办中心，本组只剩供给与管理 |
| 👥 用户 | 用户 · 用户标签 · 运营发布身份 | 发布身份自「运营」组迁入（本质是运营的马甲用户） |
| 💰 订单与资金 | 兽医订单 · AI 订单 · 支付记录 · 兽医月结 · 红色超额 | 钱相关自「运营」抽出集中；`[2026-09-07 修订]` Toko 订单履约/对账不再抽入本组，按现状随电商整体保留 |
| 🛍 商城 | 商品 · Banner · 库存 · 库存流水 · **Toko 订单履约** · **Toko 异常订单** · **Toko 退货审核** · 开封判例 · 复购/毛利/周转报表 · **对账** · 运费配置 | `[2026-09-07 修订]` 撤销"履约/对账抽出集中"的分组方案——用户拍板 Toko 订单履约与对账维持现状归属，跟着电商整体不拆；`[2026-09-08 修订]` 退货审核/异常订单原方案收进待办中心组，用户看真实后台侧栏截图后要求一并改回随电商整体归入本组——现状后台侧栏电商组本就含这两页，此前按"待处理事项"功能属性单独归类是分析角度的划分，非代码现状，故予撤销；两页页面形态仍沿用模板 A（处置工作台），只是导航归属挪动 |
| 🩺 兽医与问诊 | 兽医账号 · 在线状态 · 未成功请求 · 评分 · 历史会话查询 | 两组并一组，兽医线延期期间折叠置后 |
| ⚙️ 配置与安全 | 运营配置（含 AB-18A）· 算法参数 · 操作审计 · 后台账号（AB-16A） | 低频高危集中置底 |

**配套动线优化（同拍板，零功能改动）：** ① 待办类导航项挂未处理计数角标；② 权限勾选面板按新导航组重排；③ 报表类页面统一只读视觉标识，与处置类区分。

### ② 页面形态升级（2026-09-01 全部拍板）

**方向：告别「每页一个浅列表、从上到下」的现状形态**——63 个页面收敛为五种形态模板，UI 只出五套模板稿 + 逐页填充：

| 模板 | 形态 | 适用页面 |
|---|---|---|
| **A · 处置工作台** | 双栏：左=队列列表（紧凑行/状态色点/键盘可上下），右=选中项完整详情+处置操作区；顶部队列内筛选+计数；**处置完成自动选中下一条**（收件箱式清队列） | 待办中心组 6 页：人工复核、被举报用户、举报队列、异常工单、客服工单、退款；另外 Toko 退货、Toko 异常订单同样沿用本模板，但导航归属在商城组（`[2026-09-08 修订]`） |
| **B · 管理列表** | 顶部筛选栏 → **统计摘要条**（本列表 3~5 个关键数字）→ 表格 → 点行**右侧滑出详情抽屉**（详情+操作均在抽屉内，不跳独立浅页）；既有独立浅详情页（user-detail / content-detail 等）**并入抽屉** | 内容管理、评论、用户、用户标签、内容标签、顶置、定时发布、场所管理（AB-17A）、商品、Banner、库存+流水、订单履约、开封判例、兽医账号、未成功请求、兽医订单、AI 订单、支付记录、月结、对账、红色超额、发布身份、历史会话、操作审计、后台账号等 ≈20+ 页 |
| **C · 报表** | 图表卡在上 + 维度/时间切换 + 明细表在下（AB-15A 看板为首例）；统一「只读」视觉标识 | 内容统计、复购效果、销售毛利、库存周转、在线状态、兽医评分 ≈6 页 |
| **D · 配置页** | 业务分组卡片 + 行内编辑 + **每卡自带保存钮**（未修改禁用 → 修改激活标「已修改」→ 高危确认 → 提交；`[2026-09-01 修订]` 原全局吸底保存条被否，回归现状每组各自提交、更防呆）；组头不标红（危险感由确认弹层承载，2026-09-01）+ data-confirm | 运营配置（含 AB-18A）、算法参数、运费配置、复核设置 ≈4 页 |
| **E · 多步工作流** | 现状骨架保留（建批次→编辑→预览→确认），只做视觉统一 | 批量内容工作台、种子发布 |

- 模板 A 窄屏降级为「列表/详情两屏切换」（已知取舍：移动端看后台体验弱于桌面）
- **交付方式（2026-09-01 拍板）：整包随 1.3.0**，不分批滚动套用。⚠️ 这是后台侧最大单块工作量（40+ 页面前端重构，零后端功能改动），排期时单独计算

### ③ 随重构的功能级微调（例外清单，2026-09-03 起累计）

原则上本条「不动任何功能」，以下为产品明确拍板的少量例外，验收按本清单核对：

1. **警告处置增加必填理由**——现状 `warn` 端点无理由参数；增加 reason 必填，仅写入审计日志（`ACCOUNT_WARNED` detail）便于追溯，用户端通知文案不变
2. **用户详情·宠物档案增列 性别/生日**——PetProfile 数据已有，纯读展示
3. **排期管理并入批量内容页签「排期发布」**——端点不变，导航项撤销
4. **标签打标/分配入口收进标签抽屉分配页签**——内容标签「给内容打标」与用户标签「批量分配」独立按钮废除，端点不变，多选/顶掉能力保留
5. **待办中心 8 页统一「待处理｜已处理」两态**——已处理只读回看，复用现状 status 字段
6. **全站搜索统一模糊匹配**、导出文件按字段分列（9-7 规则 12）
8. **兽医「在线状态」「兽医评分」两独立只读页并入兽医账号详情抽屉**（列表补最后在线列）；**PawCoin 充值档位新增「新建档位」交互（代码仅有启停端点，无新建接口，需新排期，本条只是设计意图先行）**
9. **PawCoin 充值档位列表改为只展示启用中档位（≤4 档）**，停用档位不在列表出现（2026-09-04 拍板）——**待确认：停用后的档位如何重新启用？** 现状代码 `setTierEnabled` 可反复切换，但若列表不展示已停用行，运营会失去「重新启用」的操作入口，需要产品补充方案（如「查看已停用」展开区），否则停用会变成不可逆的单向操作
10. **算法参数页去 FR 编号、分组名「打分参数」改「算法参数」、11 项参数全集改一行一项展示**；**变更记录由常驻表格改为按钮点击查看**（历史记录移入抽屉，支持按参数/时间段筛选）
11. **兽医账号新增「开户」完整流程与「运营真实账号纳入」完整流程的 UI 设计**（此前 PRD 与设计稿均只提及端点/字段，未画出交互流程，2026-09-04 补齐）
7. **警告增加必填理由**（见 1 条，仅入审计）；**用户详情·宠物档案增列性别/生日**（见 2 条，PetProfile 数据已有）；**内容/用户标签码统一为系统自动生成不可填**（`ct-<id>`/`ut-<id>`，防撞码，2026-09-02 代码已改，PRD 首次记载）；**内容/用户标签的分配记录补展示起止时间与生效/到期状态列**

### ④ 验收与前置

- **不动任何功能**——不增删任何页面能力与操作端点；重整前后按全量清单**逐操作核对不丢一项**，此为核心验收标准
- **三语齐全**——重构涉及的每个新增/改动 message key 必须 zh/en/id 三语齐备（对照基线包做缺失校验），缺任一语言不得合入
- **final 前重新盘点（2026-09-01 拍板）**：工程仍在持续改代码，本 PRD 定稿（status: final）前须对 `dev/dev_1.1.6` 最新代码**重新跑一遍盘点 diff**，把清单与本节对齐到当日现状
- 本条是其余四条 AB 的 **UI 前置**：AB-15A/16A/17A/18A 的 UI 稿按新 IA 与新页面形态出，避免按旧导航画完返工

---

## 6. AB-20A：虚拟账号评论（暖贴）（2026-09-03 新增）

**动机：** 早期社区帖子评论稀薄，作者发帖无人回应会流失。现状运营只能在 App 端一个帖子一个帖子点开看谁没评论，再手工切虚拟账号回复——不可规模化。本条把「找到冷帖 → 用虚拟账号评论」收进后台一条动线。

**页面位置与流程：** 「内容 → 评论管理」页升级为两页签；另在「待办中心」新增「暖贴回复跟进」队列页（见 ③）：

- **页签一「评论巡查」**＝现状评论列表与下架/恢复，功能不变。
- **页签二「帖子评论分布」（本条新增）**＝帖子维度视图，运营在这里找冷帖并直接评论。

### ① 帖子评论分布（页签二）

- **筛选：** 评论数区间（快捷档 `= 0` / `≤ 3` / 自定义 `≤ N`）+ 发布时间段 + 物种 + **「排除虚拟账号内容」开关（默认开，文案 2026-09-03 定稿）**——只排除虚拟账号池发布的帖子（真实身份池的帖照常参与），保证运营的暖评火力集中在真实账号的帖子上
- **列表列：** 帖子摘要 ｜ 作者 ｜ 发布时间 ｜ 评论数（0 红标 / 低量黄标）｜ **其中虚拟评论数** ｜ 操作「去评论」
- **数据展示（页面常驻摘要条，用于运营数据分析）：** 筛选范围内帖子数 · **篇均评论数（双口径并列：含虚拟 / 剔除虚拟）** · 0 评论帖数与占比 · 虚拟评论占比。口径＝当前筛选结果集、可见评论（已下架评论不计）；剔除虚拟口径＝评论作者 ∉ 虚拟账号池——保证暖评动作不污染真实互动数据（2026-09-03 确认）
- **排序：** 默认评论数升序（最冷的排最前）
- **列表范围：** 帖子状态为普通筛选项（全部/可见/已下架，默认全部）——**给哪些帖暖评由运营自行筛选判断，页面不做预设**（2026-09-03 拍板，撤回此前「只列可见帖」的预设）

### ② 「去评论」抽屉

- **结构：** 帖子预览（图+摘要+作者+物种+当前评论数）→ 已有评论列表（含虚拟评论标识，后台可见、App 端不标）→ 常驻评论区
- **评论区：** 评论身份下拉（取「运营发布身份」池中**启用中**账号，默认只列与帖子物种匹配的，可切「全部」；每项带**「今日已评 N 条」**帮运营换手、防单账号过度使用被识破）+ 评论文本框（长度上限与 App 端一致）+ 发布按钮（身份未选或内容为空禁用——F7 规则 11）
- **发布语义：** 走 App 评论同链路——作者收到普通评论推送/通知、评论计数与**推荐算法互动权重照常生效（会真实推高帖子曝光，2026-09-03 产品确认接受）**；App 端该评论与真实用户评论**无任何视觉区别**。本期只做评论，虚拟点赞不做（2026-09-03 拍板，后续版本再议）
- **连发体验：** 发布成功 toast 后抽屉不关闭、文本框清空，可为同帖继续评或点「下一帖」；列表评论数列即时 +1
- **防呆：** 同一虚拟账号对同一帖 10 分钟内再次评论时，行内提示「该账号刚评过这帖，确认再发？」（不弹层，二次点击即发）；**同帖虚拟评论已 ≥3 条时评论区上方黄条提示**（不拦截——官托观感自查）

### ③ 暖贴回复跟进（待办中心新页，闭环「已读不回」）

真实用户回复了虚拟评论后，虚拟账号侧无人盯会变成「作者跟不会回话的账号说话」，暖贴反噬。故：

- **页面位置：** 「待办中心」新增第 8 页「暖贴回复跟进」，导航角标计数
- **入队规则：** 任一虚拟评论收到新回复（含二级回复）即生成一条待跟进项；同一虚拟评论的多条新回复合并为一项
- **工作台（模板 A 双栏）：** 左=待跟进队列（虚拟账号/帖子/回复摘要/时间）；右=帖子卡 + 评论线程（新回复高亮）+ 常驻回复区——**回复身份锁定为被回复的那个虚拟账号，不可切换**（防串人设）
- **处理动作：** 发布回复（复用 App 评论回复链路，回复者照常收通知）或 标记已读（不回复）；处理后出队并自动选中下一条
- **页签：** 待跟进 / 已跟进（近 30 天）

### ④ 工程与安全

- **新端点：** 帖子评论分布聚合查询（分页）+ `POST /admin/comments/virtual`（postId + virtualAccountId + text，幂等 token；回复时带 parentCommentId）+ 暖贴回复跟进队列查询/标记已读
- **权限：** 新权限码 `comment.virtual_post`（默认超管；可单独授予运营）；虚拟账号池本身仍归 `virtual_account.manage`
- **审计：** 每条虚拟评论落审计（动作码 `COMMENT_VIRTUAL_POST`，记录 postId/虚拟账号/文本摘要）
- **数据边界：** 「排除虚拟账号内容」的判定=作者 ∈ 虚拟账号池（仅此，2026-09-03 拍板）；虚拟评论标识只在后台展示，不落 App 端任何接口字段

### ⑤ 与既有机制的关系（并存，不替换）

评论管理页签一、内容管理、虚拟账号池的既有功能全部不动；本条只新增页签二与评论端点。虚拟账号的创建/启停/物种标记仍在「运营发布身份」页维护。

---

## 7. AB-21A：角色配置功能（2026-09-04 新增）

**动机：** 核实代码发现后台"改角色"只是给账号从几个写死的角色里选一个赋值——角色本身（角色叫什么、包含哪些权限码）是硬编码在 Java 枚举 `AdminRole` 里的（如 `OPS_MANAGER(List.of(CONTENT_VIEW_REPORTS, ..., VET_QUALIFY, ...))`），新增角色或调整某角色的权限范围只能改代码重新发版，运营在后台完全无法自主配置。三个关键决策点已与 Hex 讨论确认：

1. **只做可视化管理，不重新设计权限模型**——沿用代码里现有的全部权限码（约 30+ 个，如 `content.takedown`、`user.grant_pawcoin`），新角色 = 从这些权限码里勾选组合，不新增/拆分权限颗粒度
2. **完全不碰超管护栏**——`SUPER_ADMIN` 继续硬编码、隐式全权、上限 5 人不变；本功能只管理 SUPER_ADMIN 之外的角色
3. **现状固定角色迁移为系统预置角色**——**2026-09-04 核实代码后修正名单**：`AdminRole` 枚举实际有 6 个非超管角色，分别是 OPS_MANAGER（运营主管）/ OPERATIONS（运营专员）/ FULFILLMENT（发货专员）/ SUPPORT（客服）/ FINANCE（财务）/ CUSTOM（自定义，见下方架构发现）——此前草稿把角色名单记错并编造了一个不存在的"兽医管理员"，已改正。经与产品确认：**运营主管（OPS_MANAGER）不迁移，代码里原样保留不动**；迁移为系统预置角色的是 **运营专员／发货专员／客服／财务** 四个

### ⚠️ 重要架构发现（2026-09-04 核实代码，待与产品确认技术路线）

`AdminRole` 枚举自身的类注释写明：**运营专员/发货专员/客服/财务这类"岗位角色"的权限是运行时从 Java 代码实时解析的，从不写入 `admin_account_permissions` 表**；表里落库的权限**只有 `CUSTOM` 角色才有**——`CUSTOM` 正是现状"编辑权限"功能（admin-accounts.html 的 `editPerms`/`savePerms`）背后的机制：给单个账号逐条勾选权限码、存进数据库，行为与本条 AB-21A 设想的"角色模板"高度相似，但**颗粒度是账号级，不是角色级**（两个账号都想要同一份权限，现在只能各自勾一遍，互不关联、编辑一个不影响另一个）。

这意味着 AB-21A 要把"运营专员"这类岗位角色的权限从**代码里的固定列表**变成**运营能在后台编辑的东西**，工程上不是简单加一个管理页面，而是要把这些角色的权限解析路径从"读 Java 枚举"改成"读数据库"——这一步比初始预想的工作量大。技术路线上至少有两个方向，需要产品定：

- **方案 A：新建角色-权限表**，运营专员/发货专员/客服/财务四个预置角色的权限从枚举迁移进新表，运行时改为查表解析；`CUSTOM` 机制保留不变、两套并存
- **方案 B：复用/泛化现有 `CUSTOM` 机制**，把"账号级权限勾选"升级成"角色模板级权限勾选"（角色模板表 + 账号引用模板 id），四个预置角色和未来的自定义角色统一走这一套，`admin_account_permissions` 表逐步废弃

本 PRD 暂不选定方案（技术方案由工程侧评估，产品只约束外部行为——见 §① ②），但请知悉这条改动比表面看到的"加一个页面"更深，评审排期时需要说明。

**页面位置与流程：** 「安全 → 后台账号」下新增子页「角色配置」，导航与"后台账号"并列。

### ① 角色列表

- **列：** 角色名称 ｜ 类型（系统预置 / 自定义）｜ 权限数 ｜ 使用中账号数 ｜ 操作
- 系统预置角色（运营主管/运营专员/兽医管理员/客服，具体清单见现状 `AdminRole` 枚举去除 `SUPER_ADMIN` 后的全部）**可编辑权限，但不可删除**（假设，见待确认）；自定义角色可编辑可删除（删除前必须先把使用中账号转移到别的角色，data-confirm 复述受影响账号数）

### ② 新建/编辑角色

- 角色名称（自定义角色必填且可改；预置角色名称不可改，避免和历史审计记录里的角色名对不上）
- **权限勾选：** 页面维度矩阵，每个后台页面固定“查看”+“编辑”两档；个别页面存在超出这两档的独立操作权限（如用户页的停用/删除/赠送 PawCoin 是三个独立权限，不是一个通用“编辑”），单列“其他操作权限”栏位如实列出，不强行归并成假的两档模型。数据来源=代码里唯一的权限码定义类 `AdminPermissions.java`（72 个常量），覆盖全部约 35 个页面维度（含此前遗漏的待办中心工单/客服/退款/Toko退货/异常订单、商城财务、用户手机号等）。**代码现状两处不规整，随本功能梳理时一并发现，非本功能引入**：评论管理没有独立的查看权限码（沿用 content.view）；退款管理与 Toko 退货共用同一套 refund.* 权限码，无法只放开一边——建议工程评审时确认是否需要拆分，本 PRD 不擅自定，如实记录待讨论。
- 保存=高危确认（复述新增/移除的权限项，影响该角色下所有账号，立即生效）

### ③ 与既有机制的关系

- 「后台账号」页的"改角色"下拉改为从本功能的角色列表读取（含预置+自定义），行为不变（仍是给单个账号赋值一个角色）
- 现状 5 个超管上限、自我保护护栏（AB-16A）、审计记录不受影响

### ④ 待确认（假设先行，需产品拍板）

- **系统预置角色是否允许删除**——本节先假设"不可删除，只可编辑权限"，因为删除后该角色下的历史审计记录会失去可读的角色名上下文；如需允许删除，需要补充"删除时角色名转存进审计快照"的工程方案
- **迁移方式**——现有 4 个预置角色的初始权限清单需要一次性从代码迁移进数据库（一次性脚本，随本功能上线跑一次，之后角色权限以数据库为准、代码枚举退役）

---

## 8. 待解决问题

- **OQ-B1（AB-15A）：** 图表库选型——vendored Chart.js（成熟、体积可控）vs 自绘 SVG（零依赖、开发量大）。技术方案定，PRD 只约束「无 CDN 外链」。
- **OQ-B2（AB-15A）：** 日粒度预聚合的跑批失败补偿（某日跑批漏了如何回补）——技术方案定，PRD 只约束「任一天数值与 SQL 直跑一致」。

## 9. 假设索引

| # | 关联 | 假设 |
|---|---|---|
| B-1 | AB-15A | 预聚合物化表按日一行、历史行不可变，与 SQL「同一天数字不随后续互动上涨」的口径天然一致，无需重算历史 |
| B-2 | AB-16A | 「会话立即失效」通过使该账号全部活跃 session 无效实现（现有 session 管理机制支持按账号失效；若不支持，技术方案评估替代——最低要求下次请求时权限校验按新状态生效） |
| B-3 | AB-17A | 场所举报复用既有举报数据模型（举报对象类型可扩展）；不可扩展则新建场所举报表，处置流交互不变 |
| B-4 | AB-18A | KTP 定价所用的后台配置机制（Story 9.2）支持按 key 扩展新配置项；不支持则在既有配置页内新增同型实现，交互不变 |
