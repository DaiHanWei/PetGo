# TailTopia 运营后台功能全量清单（2026-09-01 按代码盘点）

**盘点口径：** `petgo-platform` 分支 `dev/dev_1.1.6` @ `7ac9a4dd`（当前功能超集，含电商线全量；2026-09-01 final 前复核与远端最新一致），63 个后台页面模板 + 128 个写操作端点，逐一对照控制器提取。**本文是现状清单，服务于 1.3.0 后台重整（AB-19A）的梳理讨论**；每页格式 = 页面（路径）｜看什么｜能做什么。

**图例：** 🔴 高危操作（不可逆/涉钱/涉权限） · 🕳 无导航入口（只能页内链接或直链进入） · 🧪 仅 staging

---

## 概览

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 概览 `/admin/dashboard` | 用户/订单/内容/兽医四模块聚合数字 | 只读（→ 本版 AB-15A 替换为数据看板，7/30 天切换） |

## 内容

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 种子内容发布 `/admin/seed-post` | 单发表单 | 以运营身份发种子帖（文字+图片上传）；旧 CSV 导入入口 |
| 批量内容 `/admin/seed-batches` + 工作台 + 预览 | 批次列表/行编辑/预览 | 建批次 · 工作台行增删改/整表粘贴导入/素材上传与单张废弃 · 批次设置 · 确认导入发布 |
| 内容管理 `/admin/content` + 🕳 内容详情 `content-detail` | 全量帖列表（筛选/搜索）、单帖详情 | 下架 / 恢复 🔴 · 种子帖物种归属标记（AB-3H）· 限流 / 解除限流（FR-95 配套） |
| 评论管理 `/admin/comments` | 全量评论巡查 | 评论下架 / 恢复 |
| 人工复核 `/admin/manual-review` | **四类统一待办混排**：内容送审（放行队列，>24h 高亮）· 内容举报 · **昵称/宠物名审核** · **用户/宠物头像审核** | 送审通过/拒绝丢弃/调优先级 · 举报下架/驳回/整帖驳回 · 名称通过/违规重置（`name-moderation/{id}/decide`）· 头像通过/违规重置默认（`avatar-review/{id}/decide`）· 复核总开关（仅超管）——名称/头像审核此前无任何 PRD 记载 |
| 🕳 内容举报队列 `/admin/reports` | 内容举报待处理/已处理两表 | 下架 / 驳回 / 整帖一键驳回 / 批量驳回——**2026-08-19 被人工复核统一队列取代的遗留页**（孤儿页成因），AB-19A 建议退役、端点保留 |
| 顶置管理 `/admin/content-pins` | Feed 坑位列表 | 新建坑位 / 编辑 / 终止（AB-10A） |
| 内容标签 `/admin/content-tags` | 装饰标签与已打标内容 | 建标签 / 编辑 / 退役 / 打标 / 移除（AB-10C） |
| 🕳 定时发布 `/admin/content-schedules` | 排期列表 | 改发布时间 / 取消（AB-3L）——**无导航入口** |
| ~~内容统计 `/admin/content-stats`~~ | — | **dev 分支已撤掉整页**（commit 4bbc04e1，互动积分改为内容管理列表的点赞列）——AB-3G 整页形态退役 |

## 用户运营

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 用户 `/admin/users` + 用户详情 | 用户列表/单人全景 | 停用 / 恢复 🔴 · **删除用户** 🔴（注销）· **赠送 PawCoin** 🔴（补偿用，独立权限 `user.grant_pawcoin`，此前无 PRD 记载） |
| 被举报用户 `/admin/tickets` | 账号举报工单（AB-3D） | 警告 / 封号 🔴 / 驳回 · 批量处置 · 限流/解除（联动 FR-95） |
| 用户标签 `/admin/user-tags` | 运营标签与分配 | 建 / 编辑 / 退役 / 分配 / 移除（AB-12A，单用户上限 3 个顶掉最早） |

## 兽医（兽医线延期后低频区）

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 兽医账号 `/admin/vets` + 编辑 + 资质 | 兽医列表/资料/资质 | 创建（账密）🔴 · 编辑资料/头像 · **重置密码** 🔴 · 启停状态 · 资质录入与审核通过/驳回 |
| 在线状态 `/admin/vets/online` | 实时在线监控 | 只读 |
| 未成功请求 `/admin/failed-requests` | 掉单/超时列表 | 归档 / 跟进 / 备注 |
| 兽医评分 `/admin/ratings` | 评分列表 | 只读（筛选） |

## 问诊异常（低频区）

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 异常工单 `/admin/anomalies` + 详情 | 问诊异常队列 | 备注 / 标记解决 |
| 历史会话查询 `/admin/consult-sessions` | 会话取证检索 | 查询（POST 筛选） |

## 客服

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 客服工单 `/admin/support-tickets` + 详情 | 用户求助工单 | 关联订单 · 工单内退款批准/拒绝 🔴 · 办结 |

## 退款

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 退款管理 `/admin/refunds` + 详情 | 退款队列 | 审批通过/审批拒绝 · 批准 / 驳回 · **打款** 🔴 |

## 运营

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 运营配置 `/admin/config` | 定价/开关配置 | 定价（问诊/AI/KTP HD）🔴 · PawCoin 充值档位启停 · 分享奖励参数（AB-3M）——**本版 AB-18A 增加护照样式定价组** |
| 算法参数 `/admin/algo-params` | FR-95 打分参数 | 调参 🔴（独立页，不对普通运营开放，全程留日志） |
| 兽医订单 `/admin/consult-orders` + 详情 | 咨询订单 | 订单核验（verify） |
| AI 订单 `/admin/ai-orders` + 详情 | AI 问诊订单 | 只读 |
| 兽医月结 `/admin/settlements` | 月结单 | **打款** 🔴 / 归档 |
| 支付记录 `/admin/payments` | 支付流水 | 查询 · 🧪 模拟支付回调（stag 专用，绝不合并主线） |
| 红色超额 `/admin/red-overage` | 红色评级超额用户 | 复核处理 |
| 运营发布身份 `/admin/virtual-accounts` | 虚拟账号池（AB-3I） | 建虚拟账号 · 启停 · 物种标记 · 发布身份增删 |

## 安全

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 操作审计 `/admin/audit-logs` | 哈希链审计记录 | 查询（POST 筛选）；链校验 |
| 后台账号 `/admin/accounts` | 账号列表 | 创建 🔴 · 改模块权限 🔴 · 改岗位角色 🔴（上限5/最后超管护栏）· 停用/恢复 🔴 ——**本版 AB-16A 补：改显示名 · 换绑 Lark 邮箱 🔴 · self 护栏** |

## 电商 Toko（1.2.0 并入，12 页——单组最大）

| 页面 | 看什么 | 能做什么 |
|---|---|---|
| 商品管理 `/admin/shop/products` + 表单 | 商品列表 | 建商品 / 编辑 / 图片 / SKU 管理 / 上架 / 下架 |
| 顶部 Banner `/admin/shop/banners` | Banner 列表 | 建 / 编 / 图片 / 启用 / 停用 / 删除 |
| 库存管理 `/admin/shop/inventory` + 流水 | 库存与变动流水 | 采购入库 · 退货入库 · 报损 🔴 · 盘点 🔴 |
| 订单履约 `/admin/shop/orders` + 详情 | 待发货/在途订单 | 发货 · 整单标记送达 · 按包裹标记送达 |
| 异常订单 `/admin/shop/order-exceptions` | 履约异常 | 取消整单 🔴 / 取消单行 🔴 / 继续履约 |
| 退货审核 `/admin/shop/returns` + 详情 | 退货申请 | 批准 / 驳回 · 回寄指引 · 验收通过/不通过 · **退款** 🔴 |
| 开封判例 `/admin/shop/return-precedents` | 开封退货判例库 | 录入判例 |
| 复购引擎效果 `/admin/shop/repurchase-dashboard` | 复购推荐效果报表 | 只读（POST 筛选） |
| 销售与毛利 `/admin/shop/margin` | 经营报表 | 只读（POST 筛选） |
| 库存周转 `/admin/shop/inventory-turnover` | 周转报表 | 只读（POST 筛选） |
| 对账 `/admin/shop/reconciliation` | 支付对账 | 对账执行 |
| 服务范围与运费 `/admin/shop/shipping` | 配送配置 | 配送区域增改/启停 · 免邮门槛 · 退货地址 |

## 本版本新增（1.3.0，见 `v1-3-0后台prd.md`）

- 概览 → **AB-15A 数据看板**（18 指标图表化，7/30 天切换）
- 内容 → **AB-17A 场所管理**（新页 `/admin/places`）
- 安全·后台账号 → **AB-16A**（改名/换绑/self 护栏）
- 运营配置 → **AB-18A**（护照样式定价组）

## 盘点中的新发现（此前任何 PRD 未记载）

1. **头像审核**挂在人工复核页内（`avatar-review/{id}/decide`）——用户头像也走审核，文档从未提过
2. **赠送 PawCoin**（`user.grant_pawcoin` 独立权限）——客服补偿通道，涉虚拟资产发放
3. **删除用户**（后台侧注销）与停用并存
4. **模拟支付回调**为 stag 专用测试工具（代码注明绝不合并主线）——重整时别当正式功能编排
5. 孤儿页最终名单：~~内容统计~~（dev 分支已撤页）、内容举报队列 reports（已拍板删除）、**排期管理 content-schedules（唯一存留孤儿页，重构补入内容组导航）**
