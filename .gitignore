.DS_Store
# DB schema 导出（目录重编号后路径由 数据埋点/ 变为 3.数据埋点/，改为不限路径匹配）
db-schema-reference.md

# UI 稿逐屏截图产物（本地核验用，不入库）
.shots/

# 决策记录：过程性内部文档，不入库（2026-08-10 产品决定）
# 只留在本地，供下一轮工作追溯；不限路径匹配，各版本目录下的同名文件一并忽略
.decision-log.md
.memlog.md
